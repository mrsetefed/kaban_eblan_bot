from telegram.constants import ParseMode

# Текст в HTML: в старом Markdown ломаются команды с нижним подчёркиванием вроде /kogda_dnd.
# Внутри текста не должно быть символов < > & кроме тегов.
HELP_PARTS = [
    (
        "<b>Дарова! И так, начнем с самого важного — нахуя нужен этот бот?</b>\n\n"
        "Ну во первых не пизди так про него, это мой сын вообще-то. Основная функция у него прежняя — делать для нас "
        "график игр, но с тех пор он подрос и научился всякому, так что сейчас пробежимся по всему.\n\n"
        "<blockquote>Бот живет на бесплатном хостинге, поэтому после перезапуска может отвечать ~минуту. "
        "Это НОРМАЛЬНО. Если молчит дольше 5 минут, тогда уже можно написать мне, что он упал.</blockquote>\n\n"
        "<b>1. График: как его заполнять</b>\n\n"
        "Пока никак, он захардкожен по правилам, так как у всех фиксированный. Но если понадобится можно написать /upd без параметров."
        "Больше пока не пишу, он не используется. Если вдруг понадобится и включу назад дам знать"
    ),
    (
        "<b>3. Всякая хуйня для души</b>\n\n"
        "/krutometr — раз в день (по Москве) меряет твою крутость. Результат за день один, перекрутить не выйдет, "
        "не пытайся. Если ответить этой командой на чужое сообщение, померяет того, на кого ответил."
        "/top — топ крутости недели. А в понедельник в 12:00 по Москве бот сам напишет итоги прошлой недели."
        "/mog — мерилка показателей в стиле мемов про луксмаксинг. Без всего просто померит тебя. Ответом на "
        "сообщение или тегом устроит баттл между тобой и им."
        "выдаст каждому тир от SUB-3 до TRUE ADAM и объявит, кто кого замогал. Все случайно, не принимай близко к сердцу.\n\n"
        "/taro — вытянет случайную карту таро и даст ее общее трактование. Иногда выпадает что-то особенное, "
        "но это уже сюрприз.\n\n"
        "/roll — кубики. Формат: сколько кубиков, d, сколько граней, потом + или - модификатор:\n"
        "<pre>/roll 2d6+3\n/roll d20\n/roll 3d8-2</pre>\n"
        "Без параметров кинет d20. Покажет каждый бросок в скобках, а потом итог. Можно до 100 кубиков и до 1000 граней.\n\n"
        "/quote — ответь этой командой на смешное сообщение, и бот запишет его в цитатник. Просто /quote без ответа "
        "достанет случайную цитату из чата.\n\n"
        "А еще если кинуть в чат 🎰, бот иногда крутит свою в ответ. Иногда, не всегда, не наглей.\n\n"
        "<b>4. Служебное</b>\n\n"
        "/verify — покажет твою роль и ID.\n"
        "/ping — проверить, жив ли бот. Ответит pong."
    ),
]

HELP_TEXT_VLAS = (
    "Но тебя все выше вряд ли ебет, мне просто лень разделять это дело да и мало ли важно будет. "
    "Для тебя роляют только эти команды:\n"
    "<pre>/today — че там сегодня, соот-но\n"
    "/tomorrow — шо по завтра\n"
    "/week — что по всей этой недели</pre>\n\n"
    "Каждая покажет, свободен я (✅) или занят (❌), а если к дню есть комментарий, то и его, типа "
    "«✅ свободен — рано уйду». По умолчанию я свободен всегда, занят только в те дни, где это отмечено.\n\n"
)


async def help(update, context):
    user_id = str(update.effective_user.id)
    from utils import get_user_role  # если не импортирован заранее

    roles = get_user_role(user_id)
    for part in HELP_PARTS:
        await update.message.reply_text(part, parse_mode=ParseMode.HTML)
    # Отдельно для vlas
    if roles and ("vlas" in [r.lower() for r in (roles if isinstance(roles, list) else [roles])]):
        await update.message.reply_text(HELP_TEXT_VLAS, parse_mode=ParseMode.HTML)
